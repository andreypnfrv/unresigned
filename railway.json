{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "yarn install && (cd ckEditor && yarn build) && yarn build"
  },
  "deploy": {
    "startCommand": "yarn next start -H 0.0.0.0 -p ${PORT}"
  }
}
